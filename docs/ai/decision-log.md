# Decision Log

Format:
- YYYY-MM-DD | Area | Decision | Reason | Consequences

- 2026-05-13 | Detection pipeline | Add a separately trained YOLO one-class `swimmer` detector before VitPose++ | GT bbox evaluation overestimates real inference quality; YOLO should provide bboxes for the top-down pose model | YOLO training is prepared from GT bbox annotations in a dedicated `script_old/yolo_training/` workflow; future VitPose++ evaluation should compare GT bbox vs YOLO bbox pipelines.
- 2026-05-13 | Dataset bbox convention | Store Side_above_water GT bboxes with `0.10` padding in the prepared VitPose++ dataset and keep YOLO conversion padding at `0.0` | Original GT bboxes were visually too tight; adding padding once makes YOLO and VitPose++ train on the intended margin while avoiding double expansion | Regenerate dependent YOLO datasets after VitPose++ dataset regeneration; YOLO-predicted bboxes should be passed downstream as-is unless a new padding convention is deliberately trained and evaluated.
- 2026-05-14 | Dataset bbox convention | Increase Side_above_water prepared GT bbox padding from `0.10` to `0.20` | Visual inspection indicated bboxes should be less tight before both YOLO and VitPose++ training | VitPose++ train/val/test and the derived YOLO dataset were regenerated; YOLO conversion still uses `bbox_padding_ratio=0.0` to avoid double padding.
- 2026-05-14 | Dataset bbox convention | Use anisotropic GT bbox padding: horizontal `0.20`, vertical `0.25`, minimum `15 px` per side | The swimmer needs more vertical context while retaining controlled horizontal expansion | Preparation scripts now default to anisotropic padding; regenerate VitPose++ and derived YOLO datasets before the next training run.
- 2026-05-14 | VitPose++ training observability | Add an optional current-status text file updated during training | Long-running tmux jobs need a quick progress check without parsing logs or attaching to the session | `src/vitpose_base/tools/train.py` accepts `--status-file` and `--status-interval`; the hook overwrites one status file with current epoch, iteration, percent, ETA, and work dir.
- 2026-05-20 | SwimXYZ visibility semantics | Treat SwimXYZ `z` as a 3D coordinate, not as keypoint visibility/confidence | The active conversion pipeline documents `z` as a Blender-space coordinate and reconstructs COCO/MMPose visibility from image position instead of consuming `z` | In the prepared pipeline, visibility is currently collapsed to `v=0` for missing/border-clamped points and `v=2` for retained points; `v=1` occlusion is not preserved unless a separate source field is introduced.
- 2026-05-15 | VitPose++ checkpoint policy | Keep only latest periodic checkpoint plus best validation checkpoint | Each VitPose++ checkpoint is about 11 GB, so retaining every epoch is too expensive while best/latest are enough for resume and model selection | Launchers/config generation use `checkpoint_config.max_keep_ckpts=1`, `create_symlink=True`, and `evaluation.save_best='AP'`.
- 2026-05-15 | VitPose++ resume robustness | Ignore invalid checkpoint metadata config during resume while still loading model/optimizer/epoch state | A resumed checkpoint contained an unparsable `meta.config`; failing only on metadata blocked otherwise valid incremental training | `runner.resume` is wrapped so invalid metadata config is tolerated; future resume tests should still verify model state and metrics continuity.

- 2026-05-16 | VitPose++ checkpoint policy | Keep best validation checkpoint plus the latest three periodic checkpoints (max_keep_ckpts=3) | Continuing beyond epoch 30 may overfit, so training needs a small rollback window without retaining every 11 GB checkpoint | Future launchers/config generation preserve best_AP_*.pth, latest.pth, and the two previous periodic checkpoints while deleting older periodic checkpoints.
- 2026-05-20 | YOLO+VitPose++ pipeline | Consolidate on `YoloVitPose_mAP` with `script_old/yolo_training/evaluate_yolo_vitpose_map.py` and MMPose-only predicted skeleton visualization | Previous saved YOLO+VitPose keypoints were not reproducible despite identical YOLO bboxes; the current pipeline correctly converts YOLO `xyxy` to COCO `xywh` and lets MMPose perform the top-down crop/affine | Old failed artifacts and custom/mixed visualization scripts were removed; full-test mAP must be regenerated before reporting end-to-end metrics.
- 2026-05-21 | Naming | Remove the redundant head-count qualifier from active VitPose++ pipeline/config/script names | The head-count choice is now part of the consolidated model setup and no longer needs to be encoded in filenames | Active preparation script is `script_old/prepare_swimxyz_vitposepp.py`; active config is `swimxyz_vitposepp_huge.py`; old naming should not be reintroduced in current workflow references.
- 2026-05-21 | Dataset preparation architecture | Rename the active prepared COCO keypoint dataset from `_train_vitposepp` to `_train_canonical` | The prepared COCO17 dataset is now the shared canonical source for VitPose++, YOLO detection export, and planned YOLO26x-pose export, not a VitPose++-only artifact | Active scripts and documentation should read from `data/intermediate/Side_above_water/_train_canonical/`; model-specific dataset exporters should derive their own output roots from this canonical dataset.
- 2026-05-21 | Dataset preparation architecture | Use `_train_canonical` as the only coordinated frame-selection source and export model-specific labels/configs into `_VitPosePP`, `_Yolo26x_detection`, and `_Yolo26x_pose` | VitPose++, YOLO26x detection, and YOLO26x pose must not independently drop frames or manipulate images after canonical preparation | Downstream exporters operate on labels/config only (`image_operations: none`); generated split counts must match the canonical train/val/test frame set.
- 2026-05-21 | YOLO26x pose training | Keep YOLO26x pose separate from YOLO detection and VitPose++ with its own `_Yolo26x_pose` dataset root, symlinked images, and `yolo pose train` launcher | Ultralytics pose expects paired `images/` and `labels/` folders with 56-field COCO17 pose labels, while the existing detector labels are bbox-only | YOLO26x-pose can train from `data/intermediate/Side_above_water/_Yolo26x_pose/swimxyz_side_above_water_yolo26x_pose.yaml`; pretrained pose weights must be provided explicitly or resolved with `MODEL=yolo26x-pose.pt`.
- 2026-05-21 | YOLO26x pose checkpoint policy | Mirror the VitPose++ checkpoint/logging policy where Ultralytics permits it: save `best.pt` and `last.pt`, set `save_period=1`, prune periodic `epoch*.pt` files to the latest three after training, write a timestamped log and run status file | YOLO does not expose `max_keep_ckpts`, but periodic checkpoint retention is still needed for rollback without unbounded disk growth | `script_old/yolo_training/train_yolo_pose_side_above_water.sh` calls `script_old/yolo_training/prune_yolo_epoch_checkpoints.py` after successful training; long runs should use the logged status and retained checkpoints for resume/debug.


- 2026-05-21 | Metrics reporting | Standardize end-of-test reporting for pose models to include both mAP50 (AP50) and mAP50-95 (AP) | Comparing runs is ambiguous if only one threshold is tracked; we want both coarse (50) and strict (50-95) quality signals | VitPose++ training can run a final test eval via --final-test writing <work_dir>/test_metrics.json; YOLO26x-pose launcher runs a test-split eval and writes <run_dir>/reports/test_metrics.csv with both mAP50 and mAP50-95 for box and pose.
- 2026-05-23 | YOLO26x-pose hyperparameter search | Run a local 2x2 train/val-only search varying only lr0 and imgsz, with optimizer=AdamW instead of optimizer=auto | Historical Ultralytics logs show optimizer=auto ignores lr0; AdamW is the optimizer type auto selected in prior YOLO26x-pose logs, so this makes lr0 effective while preserving the rest of the standard project settings | Search script is script_old/yolo_training/yolo26x_pose_grid2x2.py; output root is runs/hparam_search/yolo26x_pose/; test split is explicitly excluded from hyperparameter selection; save_period=-1 disables intermediate epoch checkpoints so each run keeps best and last/latest only.
- 2026-05-23 | Tooling | Avoid Codex sandbox (bubblewrap / `bwrap`) on this server until further notice | The server does not permit the sandbox networking/namespace operations required by bubblewrap, causing intermittent failures when patching/reading via sandboxed helpers | Prefer non-sandboxed commands and minimal, targeted scripts for memory-only edits; document any such workaround explicitly in handoff/session notes when used.

- 2026-05-23 | VitPose++ hyperparameter search | Run a local 2x2 train/val-only search varying only learning rate and horizontal crop size | SwimXYZ swimmers are horizontal and bboxes are already padded, so the next VitPose++ tuning should focus on lr and crop geometry rather than bbox expansion or broad augmentation changes | Search script is `script_old/hparam_search/vitposepp_huge_grid2x2.py`; output root is `runs/hparam_search/vitposepp_huge/`; crop sizes are interpreted as width x height for MMPose `image_size=[W,H]` and backbone `img_size=(H,W)`; test split is excluded from selection; checkpoint retention keeps best plus latest only.

- 2026-05-23 | YOLO detector hparam search | Consolidate YOLO26x detector to `yolo26x_detector_v2/cfg_03_lr0_0.00067_imgsz_768` for the YOLO->VitPose++ pipeline | Selection is val-only and prioritizes recall; AP75/IoU are unavailable; cfg_03 ties cfg_04 on reported metrics and is chosen as the canonical best | Use `runs/hparam_search/yolo26x_detector_v2/cfg_03_lr0_0.00067_imgsz_768/weights/best.pt` as the detector checkpoint in `YoloVitPose_mAP` runs.
- 2026-05-23 | YOLO26x detector training | Train detector weights incrementally from selected `cfg_03_lr0_0.00067_imgsz_768/weights/last.pt` with `imgsz=768`, `lr0=0.00067`, and `patience=2`, saving runs under `runs/yolo26x_bbox_side_above_water/` | The hyperparameter search selected cfg_03 and the next training phase should extend the latest detector state while stopping shortly after validation mAP50-95 plateaus/overfits despite training loss improvement | `script_old/yolo_training/train_yolo_side_above_water.sh` auto-resumes from the run `weights/last.pt` when present, keeps latest three periodic epoch checkpoints, and writes a run `training_status.txt`.
- 2026-05-23 | YOLO26x detector prediction postprocessing | Keep one detector bbox per frame, selected by highest confidence and then larger area on ties | Some qualitative outputs contained multiple swimmer detections per frame; the downstream top-down VitPose++ pipeline expects a single bbox provider | `script_old/yolo_training/yolo_detection_utils.py` centralizes selection; detector overlay/evaluation scripts use this rule before drawing or passing bboxes downstream.
- 2026-05-24 | YOLO26x-pose hyperparameter search | Select cfg_04 (model 04) as the grid-eval winner for downstream training | Grid evaluation indicates cfg_04 is the best val-only choice among the 2x2 lr0/imgsz points | Downstream pose training should start from cfg_04 `weights/last.pt` (or `best.pt` if explicitly preferred) and record any further fine-tuning as a separate experiment.
- 2026-05-27 | VitPose++ post-grid continuation | Prepare the next VitPose++ run to start from `runs/vitposepp_side_above_water_aniso_20x25_min15/best_AP_epoch_35.pth` via `load_from`, not `--resume-from`, once `runs/hparam_search/vitposepp_huge/best_config.json` is completed | The grid search may change learning rate and crop size; resuming optimizer/epoch state from the old run could silently preserve stale optimization settings | `script_old/hparam_search/prepare_vitposepp_grid_best_resume.py` and `script_old/run_train_side_above_water_from_grid_best_tmux.sh` block until the grid writes a completed best config, then generate a fresh config/work dir with the selected lr/crop settings applied cleanly.
- 2026-05-27 | VitPose++ hyperparameter search | Select `cfg_02_lr_0.00100_crop_384x128` as the grid-eval winner for downstream training | `runs/hparam_search/vitposepp_huge/best_config.json` now records `status=completed` with the highest validation Keypoint AP@[OKS 0.50:0.95] among completed configs | The deferred launcher can now generate `swimxyz_vitposepp_huge_grid_best_from_best35.py` immediately and start a fresh run from `best_AP_epoch_35.pth` using `lr=0.00100` and crop `384x128`.
- 2026-05-28 | VitPose++ launch simplification | Remove the temporary post-grid trigger machinery and keep a direct tmux launcher bound to the finalized grid winner | The grid evaluation is already complete, so trigger scripts no longer add value and only increase failure surface | Keep `script_old/run_train_side_above_water_from_grid_best_tmux.sh` as the direct entrypoint; it uses `swimxyz_vitposepp_huge_grid_winner_resume.py`, which loads winner best checkpoint `best_AP_epoch_5.pth` from `cfg_02_lr_0.00100_crop_384x128`.
- 2026-05-27 | AI memory hygiene | Record only durable decisions, important project facts, and important results in `docs/ai/` | The project memory must stay useful for handoff with limited context; transient attempts that are later discarded create noise and can mislead future sessions | Do not consolidate scratch states, abandoned commands, or discarded artifacts; remove or supersede temporary notes once the final useful state is known.
- 2026-06-03 | VitPose++ evaluation | Evaluate the current VitPose++ winner on a deterministic random 50-frame subset sampled uniformly from `Side_above_water_VideoTest2` Train/Val/Test | Fast diagnostic check requested on the union of all three splits without changing the full benchmark workflow | Subset root: `data/intermediate/Side_above_water_VideoTest2/_train_canonical/subsets/random50_seed20260603/`; launcher: `script_old/run_vitposepp_video_test2_random50_eval_tmux.sh`; evaluation checkpoint: `runs/vitposepp_side_above_water_grid_winner_resume/best_AP_epoch_24.pth`; result: AP `0.83550`, AP50 `1.00000`, AP75 `0.92331`, AR `0.85800`.
- 2026-06-03 | VitPose++ evaluation | Repeat the VideoTest2 diagnostic on a deterministic random 300-frame subset sampled uniformly from `Side_above_water_VideoTest2` Train/Val/Test | Larger subset requested to sanity-check whether the 50-frame result held at a more stable sample size | Subset root: `data/intermediate/Side_above_water_VideoTest2/_train_canonical/subsets/random300_seed20260603/`; launcher: `script_old/run_vitposepp_video_test2_random50_eval_tmux.sh` with `NUM_FRAMES=300`; evaluation checkpoint: `runs/vitposepp_side_above_water_grid_winner_resume/best_AP_epoch_24.pth`; result: AP `0.81084`, AP50 `0.97781`, AP75 `0.88959`, AR `0.84800`.
- 2026-06-03 | VitPose++ visualization | Render subset overlays from saved `result_keypoints.json` rather than recomputing predictions | The saved prediction JSON is the reproducible source for the 300-frame Test2 diagnostic, and a dedicated export script is clearer for future reruns | New helper: `script/vitpose_training/vitpose_generate_test_overlays_from_json.py`; output directory: `data/intermediate/Side_above_water_VideoTest2/_train_canonical/reports/test_overlays/vitposepp_video_test2_random300_best_AP_epoch_24/`.
- 2026-06-04 | Dataset aggregation | Rebuild `data/intermediate/Side_above_water_EntireSwim/` from the complementary canonical datasets `Side_above_water` and `Side_above_water_VideoTest2`, then regenerate `_VitPosePP`, `_Yolo26x_detection`, and `_Yolo26x_pose` from the aggregate canonical root | The two canonical datasets share structure and are complementary in content, and the project needs one filtered aggregate dataset plus aligned model-specific exports without duplicating image bytes unnecessarily | `script_old/prepare_entire_swim_dataset.py` now aggregates both sources with image symlinks, writes root/canonical `manifest.json` and the preparation report, and rebuilds the model exporters in one pass; heavy runs should use `script_old/run_prepare_entire_swim_dataset_tmux.sh`.
- 2026-06-04 | EntireSwim A/B split workflow | Split `data/intermediate/Side_above_water_EntireSwim/` into complementary datasets `..._A` and `..._B` with per-element Bernoulli assignment and deterministic seed control, then regenerate `_VitPosePP`, `_Yolo26x_detection`, and `_Yolo26x_pose` for each output | The project needs reusable subset datasets with the same structure and exporters as `EntireSwim`, while keeping the split reproducible and avoiding image-byte duplication | `script_old/prepare_entire_swim_ab_split.py` uses default `prob_a=0.3`, recreates canonical datasets with image symlinks, rewrites manifests/reports, and `script_old/run_prepare_entire_swim_ab_split_tmux.sh` is the standard heavy-run launcher.
- 2026-06-16 | Standalone frame dataset prep | Copy the SwimXYZ frame-preparation flow into `script/dataset_preparation-cleaning/`, translate the tutorial into Italian, and keep the tmux launcher aligned to the new directory | The frame-preparation workflow needs a dedicated home that matches the new repo organization while reusing the same canonical export rules | `script/dataset_preparation-cleaning/prepare_swimxyz_frames_dataset.py`, `script/dataset_preparation-cleaning/run_prepare_swimxyz_frames_dataset_tmux.sh`, and `script/dataset_preparation-cleaning/tutorial.md` are the new entrypoints.
- 2026-06-13 | YOLO26x-Pose SAW_frames launcher | Prepare the SAW_frames launcher from the existing cfg04-style workflow | The frame-based dataset needed a reproducible training workflow aligned with existing YOLO26x-Pose conventions | The run uses pretrained `models/pose/yolo26x-pose.pt`, `imgsz=768`, `lr0=0.00100`, early stopping on Pose mAP50-95 with `patience=3` and `min_delta=0.007`, retains best plus the last 10 epoch checkpoints, exports `reports/val_metrics_by_epoch.csv` plus plots, and writes Test outputs under `data/output/experiments/yolo26x-pose_SAW_frames_EntireSwim_20260612/`.
- 2026-06-18 | VitPose prediction crop/default detector | Use crop `384x128` for `script/vitpose_prediction/predict_vitpose_frame.sh` with the `runs/vitpose_SAW_frames_20260615` checkpoint family, and use YOLO detector `runs/hparam_search/yolo26x_detector_v2/cfg_03_lr0_0.00067_imgsz_768/weights/last.pt` for YOLO->VitPose operational pipeline defaults | The VitPose checkpoint was trained with `dataset_data_cfg.image_size=[384,128]` and `heatmap_size=[96,32]`; prediction had inherited `192x256`, causing crop/scale mismatch and unexpectedly low metrics | Prediction script exposes `--crop-size`; pipeline defaults were updated in operational launchers/scripts.
- 2026-06-17 | HPE report metrics | Use `script/hpe_report/build_hpe_report_tables.py` as the reproducible source for direct and cross metric tables in the HPE fine-tuning report | The PPTX tables depend on multiple generated GT, prediction, and Val-metric artifacts and need a headless repeatable path to regenerate XLSX/CSV checks | The report workflow reads COCO GT JSON, `kp_Test.json`, and `val_metrics_by_epoch.csv`; writes `data/output/experiments/hpe_report/hpe_report_tables.xlsx`; uses YOLO threshold `0.30`, VitPose++ threshold `0.20`, Val reconstruction `patience=3`, `min_delta=0.007`; see `docs/ai/runbooks/hpe-report-direct-cross-metrics.md`.
- 2026-06-24 | Script organization | Use `script/` as the consolidated operational script root for new work and keep the previous broad tree under `script_old/` | The previous script tree mixed consolidated workflows, duplicates, past versions, and tests; the consolidated tree was validated before cutover | New operational references should point to `script/`; use `script_old/` only for past versions, variants, or tests.
- 2026-06-24 | AI memory architecture | Deprecate `docs/ai/handoff.md` as an operational source of truth and keep it as a short compatibility pointer | The project no longer uses chat-to-chat handoff files at context saturation; long handoff notes increase token cost and stale-state risk | New sessions should use `context.md`, `task-board.md`, `decision-log.md`, `tests-and-results.md`, and runbooks for current truth; `docs/ai/sessions/` remains historical archive.
