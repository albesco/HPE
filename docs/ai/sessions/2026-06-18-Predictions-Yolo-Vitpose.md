# 2026-06-18 - Predictions_Yolo-Vitpose

- Role: Test / prediction.
- Added `script/yolo26x_pose_prediction/predict_yolo26x_pose_frame.sh`, a parametric YOLO26x-Pose prediction launcher for Test split evaluation.
- The launcher accepts checkpoint, main dataset directory, random Test sample size, optional seed, confidence threshold, output directory for KP/metrics, and overlay directory.
- For sampled runs, it creates a reproducible symlinked dataset view under the output directory so metrics, KP JSON, and overlays use the same selected Test items.
- Updated `script/yolo26x_pose_training/evaluate_yolo_pose_split.py` so `--conf` can be applied to Ultralytics validation when explicitly provided, while older callers that omit `--conf` keep their previous validation behavior.
- Validation: `bash -n script/yolo26x_pose_prediction/predict_yolo26x_pose_frame.sh`, `python3 -m py_compile script/yolo26x_pose_training/evaluate_yolo_pose_split.py`, script `--help` passed, and `_train_canonical` rejection was verified.
- Updated `--dataset-dir` semantics: it now expects the main dataset directory, for example `data/intermediate/SAW_frames`, and resolves `_Yolo26x_pose/swimxyz_side_above_water_yolo26x_pose.yaml` internally; `_train_canonical`, `_Yolo26x_pose`, and YAML paths are rejected.
- Added Italian tutorial `script/yolo26x_pose_prediction/tutorial.md` with parameters, defaults, usage examples, output layout, and manual tmux launch/management section.
- Diagnosed bad overlays from `--conf 0`: metrics were sane, but overlay/KP export drew all low-confidence candidate detections; added `--max-detections-per-image` to the evaluator and prediction launcher, with launcher default `1` and `0` available for debug-all mode.
- Added `script/vitpose_prediction/predict_vitpose_frame.sh`, a parametric VitPose++ prediction launcher for Test split metrics, `kp_Test.json`, and overlays; it accepts a VitPose++ checkpoint, main dataset directory, optional random sample size/seed, bbox confidence filtering with `conf=0` top-1 bbox behavior, output dir, and overlay dir. Static validation and `_train_canonical` rejection passed.
- Added Italian tutorial `script/vitpose_prediction/tutorial.md` with parameters, defaults, usage examples, output layout, and manual tmux launch/management section for the VitPose++ prediction launcher.
- Fixed `script/vitpose_prediction/predict_vitpose_frame.sh` config-generation failure: lightweight heredoc preparation now uses local `python3` instead of `conda run ... python -`; 1-frame CPU smoke test completed and wrote `effective_config.py`, `metrics_Test.json`, `kp_Test.json`, `result_keypoints.json`, and one overlay under `/tmp/vitpose_pred_smoke_1/`.
- Diagnosed low VitPose++ prediction performance as a crop/scale mismatch: checkpoint `runs/vitpose_SAW_frames_20260615/checkpoint/best_AP_epoch_18.pth` was trained with crop `384x128`, while prediction inherited dataset config `192x256`; `predict_vitpose_frame.sh` now defaults to `--crop-size 384x128` and writes matching `image_size=[384,128]`, `heatmap_size=[96,32]`, and backbone `img_size=(128,384)` into `effective_config.py`.
- Updated YOLO->VitPose operational pipeline defaults to use detector checkpoint `runs/hparam_search/yolo26x_detector_v2/cfg_03_lr0_0.00067_imgsz_768/weights/last.pt` in `script_old/yolo_training/evaluate_yolo_vitpose_map.py` and related launchers.
- Validation: `bash -n` passed for modified bash launchers, `python3 -m py_compile` passed for modified Python pipeline scripts, and a 1-frame CPU smoke run under `/tmp/vitpose_pred_smoke_crop384/` completed with AP `1.0` and one overlay.
